// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: time_slots.sql

package db

import (
	"context"
	"time"
)

const getTimeSlot = `-- name: GetTimeSlot :one
SELECT id, start_time, end_time FROM time_slots
WHERE start_time = $1
`

func (q *Queries) GetTimeSlot(ctx context.Context, startTime time.Time) (TimeSlot, error) {
	row := q.db.QueryRow(ctx, getTimeSlot, startTime)
	var i TimeSlot
	err := row.Scan(&i.ID, &i.StartTime, &i.EndTime)
	return i, err
}

const listTimeSlots = `-- name: ListTimeSlots :many
SELECT start_time FROM time_slots
`

func (q *Queries) ListTimeSlots(ctx context.Context) ([]time.Time, error) {
	rows, err := q.db.Query(ctx, listTimeSlots)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []time.Time
	for rows.Next() {
		var start_time time.Time
		if err := rows.Scan(&start_time); err != nil {
			return nil, err
		}
		items = append(items, start_time)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
